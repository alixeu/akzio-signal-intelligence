# Offline CLI fixtures only. No model or broker credentials are read from this file.
# AKZIO_STORE_ROOT overrides this path: use the explicit workspace path in docs/task2-debug.md.
[daemon]
store_root = "target/task2-fixture-store"
http_addr = "127.0.0.1:17342"
worker_count = 2
auto_paper = false

[execution]
experiment_profile = "fixture"
assets = ["TQQQ", "QQQ", "SOXX", "SOXL"]
market_data_feed = "iex"
transaction_cost_ppm = 0
slippage_ppm = 0
