Classify the assistant message as `final` if it is the requested deliverable, otherwise as `stall` if it is commentary, planning, waiting, or requesting input. Reply with exactly `final` or `stall`.

Message:
{message}
